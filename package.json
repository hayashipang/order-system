{
  "name": "order-system",
  "version": "1.0.0",
  "description": "訂單管理系統 - 專為蔬果飲品店設計",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:client": "cd client && npm start",
    "dev:server": "nodemon server.js",
    "dev:full": "echo '請分別在兩個終端中執行: npm run dev:server 和 npm run dev:client'",
    "build": "cd client && npm install && npm run build",
    "build:prod": "NODE_ENV=production npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install",
    "railway-build": "npm run install-client && npm run build",
    "test:local": "npm run build && npm start",
    "clean": "rm -rf client/build && rm -rf node_modules && rm -rf client/node_modules"
  },
  "keywords": [
    "order-management",
    "react",
    "nodejs",
    "express",
    "json-storage",
    "restaurant",
    "kitchen"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/james/order-system.git"
  },
  "bugs": {
    "url": "https://github.com/james/order-system/issues"
  },
  "homepage": "https://order-system.vercel.app"
}